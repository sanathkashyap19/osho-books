import { DetailsOnHoverDirective } from './details-on-hover.directive';

describe('DetailsOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new DetailsOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
